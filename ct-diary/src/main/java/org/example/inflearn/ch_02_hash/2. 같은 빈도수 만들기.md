# 청소
출처 : [인프런 - 자바 it 대기업 유제]
Level : 2

## 풀이 날짜
- 2026.02.03 -

## 나의 풀이
```java
import java.util.*;
class Solution {
	public int[] solution(String s){
		int[] answer = new int[5];
        // 먼저 해당 문자열의 최대 빈도 문자를 구한다음
        // 각값을 최대 빈수도 - 해당 문자 빈도수를 차례대로 넣으면 됨
        char[] charArr = s.toCharArray();
        
        // 각 값의 빈도수를 기록하는 map         
        HashMap<Character, Integer> map = new HashMap<>();
        
        for(int i = 0; i< charArr.length; i++){
            map.put(charArr[i], map.getOrDefault(charArr[i], 0) + 1);
        }
        
        // 최대 빈도수구하기
        int max = hash.values().stream().max(Integer::compareTo).orElse(0);
        
        // 3
        hash.values.stream().mapToInt(i -> max- i).toArray();
        
		return answer;
	}

	public static void main(String[] args){
		Solution T = new Solution();
		System.out.println(Arrays.toString(T.solution("aaabc")));
		System.out.println(Arrays.toString(T.solution("aabb")));
		System.out.println(Arrays.toString(T.solution("abcde")));
		System.out.println(Arrays.toString(T.solution("abcdeabc")));
		System.out.println(Arrays.toString(T.solution("abbccddee")));
	}
}

```

## 기술 회고
스트림 사용법
```java
// 해시의 값의 최댓값 구하가
int max = hash.values().stream().max(Integer::compareTo).orElse(0);

// 해시의 값에 특정 값을 빼서 배열로 반환하기
hash.values().stream().mapToInt(i -> max - i).toArray();

```